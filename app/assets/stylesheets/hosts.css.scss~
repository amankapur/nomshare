// Place all the styles related to the hosts controller here.
// They will automatically be included in application.css.
// You can use Sass (SCSS) here: http://sass-lang.com/

input#host_email, input#host_number_of_guests, input#host_name,
input#guest_email, input#guest_name, input#guest_desire{

		font-size: 25px;
		height: 50px;
		line-height: 25px;
		width: 70%;
	

}
h1, h2, h3, p {
	color: white;
	}

