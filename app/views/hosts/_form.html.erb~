<%= form_for(@host) do |f| %>
   <% if @host.errors.any? %>
     
      <% @hostt.errors.full_messages.each do |msg| %>
        <h2><%= msg %></h2>
      <% end %>
      
  <% end %>

  <div class="field">
    <%= f.text_field :email , placeholder:'Email...'%>
  </div>
  <div class="field">
    <%= f.number_field :number_of_guests, placeholder:'How many guests would you like to host?' %>
  </div>
  <div class="actions">
    <%= f.submit "JOIN US", class: "btn btn-large btn-info"%>
  </div>
<% end %>
